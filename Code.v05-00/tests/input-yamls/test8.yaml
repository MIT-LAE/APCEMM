# Null values mean "no override". They must stay valid and leave the
# compiled defaults untouched, at the top level and inside a submenu.
# The output folder is required, so it is the one value this file sets.
SIMULATION MENU:
  RANDOM NUMBER GENERATION SUBMENU:
  OUTPUT SUBMENU:
    Output folder (string): ./
PARAMETER MENU:
