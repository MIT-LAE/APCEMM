# Deliberately stale: written for the removed multi-case runs.
# Reading it must fail with the migration message, not "Unknown key found".
SIMULATION MENU:
  PARAM SWEEP SUBMENU:
    Parameter sweep (T/F): T
    Run Monte Carlo (T/F): F
    Num Monte Carlo runs (int): 2
